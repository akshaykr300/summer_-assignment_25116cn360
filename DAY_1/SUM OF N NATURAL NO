#include <stdio.h>

int main() {
    int n, sum;
    printf("Enter a positive integer n: ");
    scanf("%d", &n);

    if(n>0){
        sum = n * (n + 1) / 2;

    printf("Sum of first %d natural numbers is: %d\n", n, sum);

    }
    else {
        printf("the no n is negative invalid no");
    }
    
    return 0;
}

